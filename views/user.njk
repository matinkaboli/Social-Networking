<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ data.name }}</title>
  </head>
  <body>
    <img src="{{ '../profile/' + data.description.avatar }}" width="300px" height="300px">
    <h1>Name: {{ data.name }}</h1>
    <p>Username: {{ data.username }}</p>
    <p>Email: {{ data.email }}</p>
    {% if data.description.sex %}
      <p>Male</p>
    {% else %}
      <p>Female</p>
    {% endif %}
  </body>
</html>
